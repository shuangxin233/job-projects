# 压缩、解压及独立环境验证

验证日期：2026-09-08。验证对象来自“答案文件”的实际压缩包，解压到一个新的中文路径目录后执行，使用 Windows、Python 3.10.13 的独立虚拟环境；该环境没有 pip，没有安装任何第三方包，未继承作者的 LLM 配置环境变量。

| 检查 | 实际结果 |
| --- | --- |
| 完整压缩包可解压 | 通过 |
| 独立环境执行自动化测试 | 44 / 44 通过，`Ran 44 tests in 2.094s`、`OK` |
| 无密钥查看会话状态 | 通过，不发起模型请求 |
| 无密钥运行聊天 | 按预期提示配置缺失，退出码 1；没有读取作者桌面的配置来掩盖问题 |
| Windows 启动脚本 | 能找到 Python 并检查版本；读取解压目录的 .env 后正常启动、查看状态、退出 |
| 从解压副本调用真实 LLM API | 通过，真实模型调用 calculator 计算 `(12+8)*3`，工具结果为 60 |

本次单条真实 API 验证用于检查交付副本能否联网完成工具循环；此前 2026-09-07 的完整 8 项真实场景测试结果与原始证据见 [TEST_REPORT.md](TEST_REPORT.md)。未把本次单条验证说成重新执行了全部 8 项。

本次证据汇总见 [evidence/delivery_check.json](evidence/delivery_check.json)。测试使用的密钥只临时写入制作目录中的解压副本，验证后已移除，从未进入提交目录或压缩包。

## 面试官运行仍需具备的条件

- Python 3.10+，包含 sqlite3 和 ssl 标准库；无需第三方 Python 依赖。
- 运行真实聊天时，需有效 API Key 和能够访问所配置 API 的网络。
- 解压目录可写，以保存本地会话及运行记录。

具体安装、密钥配置、Windows/macOS/Linux 命令和排错步骤见 [QUICKSTART.md](QUICKSTART.md)。macOS/Linux 仅提供标准库运行说明，本次未在这些系统上实测。免费 API 的后续可用性、限流及模型回答质量不由本地项目保证。
